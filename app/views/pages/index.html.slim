h1 Listing pages

table
  thead
    tr
      th Label
      th Title
      th Description
      th Image
      th
      th
      th

  tbody
    - @pages.each do |page|
      tr
        td = page.label
        td = page.title
        td = page.description
        td = image_tag page.image_url(:thumb)
        td = link_to 'Show', page
        td = link_to 'Edit', edit_page_path(page)
        td = link_to 'Destroy', page, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Page', new_page_path
